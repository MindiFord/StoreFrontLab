@model StoreFront.DATA.EF.Status

@{
    ViewBag.Title = "Status Details";
}

@Html.Partial("_BreadcrumbPartial")

<div class="row">
    <div class="col-lg-4"></div>
    <div class="col-lg-4">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>
            <p>
                @Html.ActionLink("Edit", "Edit", new { id = Model.StatusID }, new { @class = "btn site-btn" })
                @Html.ActionLink("Back to Status", "Index", null, new { @class = "btn site-btn" })
            </p>
        </dl>
    </div>
</div>

