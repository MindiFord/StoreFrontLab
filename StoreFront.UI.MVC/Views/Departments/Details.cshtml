@model StoreFront.DATA.EF.Department

@{
    ViewBag.Title = "Department Details - " + Model.Department1;
}

@Html.Partial("_BreadcrumbPartial")

<div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-4">
        <div>

            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Department1)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Department1)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.DeptID }, new { @class = "btn site-btn" }) 
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn site-btn" })
        </p>
    </div>
</div>